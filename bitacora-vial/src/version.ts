/** Bump alongside android/app/build.gradle's versionName (and versionCode) on every
 * release, and tag the GitHub release `v${APP_VERSION}` with a `.apk` asset attached —
 * that's what src/lib/appUpdater.ts polls for. */
export const APP_VERSION = '1.4';
